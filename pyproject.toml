[project]
name = "plex-to-tautulli"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.2",
    "lxml>=6.0.2",
    "python-dotenv>=1.1.1",
    "requests>=2.32.5",
]

[project.scripts]
plex-to-tautulli = "plex_to_tautulli:__main__"

[build-system]
requires = ["uv_build>=0.7.0,<0.10.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 140
